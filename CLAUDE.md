# 校园 · 交接给下一个会话

> **这个文件是给冷启动的 AI 看的。** 读完它，你应该知道这是什么游戏、
> 代码在哪、做到哪一步、下一步做什么、哪些规矩不能破。
>
> 仓库根目录的 `抓捕计数器.py` 和几张图是另一个无关的旧项目，别动它。
> 游戏在 `campus/`。

## 这是什么

《校园》：中文、第一人称 3D、丧尸末世潜行生存。在一所中国学校里活 30 天。

**声音是唯一的货币。** 玩家做的每一件事都在发出响度，丧尸只靠听。
关门、绕路、烧一壶水、开一枪，全都在同一套声音传播里结算。
不做射击爽游，做「不敢发出声音」。

一个人开发。目前**先验证玩法**，建模和材质排在最后。

## 先读这三个

| 顺序 | 文件 | 是什么 |
|---|---|---|
| 1 | `campus/docs/设计文档-v3.md` | **所有系统的唯一真相。** 19 节 + 数值速查，1143 行。改了系统就要同步改它 |
| 2 | `campus/unity/README.md` | C# 移植的现状、怎么装进 Unity、为什么用「对拍」而不是重写断言 |
| 3 | `campus/web-m0/src/00-config.js` | **所有数值的唯一出口。** 代码里不许出现第二个地方写数字 |

`docs/` 里的 v1、v2 是归档，只在考古时看。

## 现在做到哪

| | 状态 |
|---|---|
| 网页原型 `campus/web-m0/` | ✅ 可玩。全部系统跑通，**841 条测试全绿** |
| C# 规则层 `campus/unity/Campus.Core/` | ✅ 数学 / 随机 / 事件 / 管线 / 声图 / 传播 / 听觉 / 气味 / 时钟 / 需求 / 睡眠，**53 条对拍全绿** |
| Unity 接缝 `campus/unity/UnityGlue/` | ✅ 一条命令装进工程，带自检场景 |
| 还在 JS 里没搬 | ⏳ 碰撞 / 关卡 / 校园生成 / 丧尸 AI / 供电 / 烹饪 / 战斗 / 伤势 / 特性 / 存档 |

跑测试：

```bash
cd campus/web-m0 && for f in test/*.js; do node "$f"; done   # JS，841 条
cd campus/unity  && ./verify.sh                              # 生成配置 → 导标准答案 → 对拍 → 接缝层编译检查
```

## 本地会话第一件事：把它装进 Unity 6

```bash
cd campus/unity
./install-to-unity.sh <你的Unity工程目录>     # 那一层要能看到 Assets/ 和 ProjectSettings/
```

场景里建个空物体挂 `CampusSelfTest`，按播放。Console 应该出现：

```
关着木门：到达 14.00  余量 4.00   路径 3.00 m
门开着：  到达 34.00  余量 24.00  路径 3.00 m
开局口渴 30  可用生命上限 70
醒着过 8 小时：口渴 58.6  饥饿 16.0  困乏 40.0  生命上限 25.4
```

这几个数存在 `goldens.json` 里，是 JS 引擎算的，C# 每次跑测试都对一遍。
**对上了就说明规则层在编辑器里是活的。** 对不上先查移植，不要改数值。

Unity 的编译错误和 Console 输出在 `Editor.log`：
macOS `~/Library/Logs/Unity/Editor.log`，Windows `%LOCALAPPDATA%\Unity\Editor\Editor.log`。

## 接下来的工作顺序

移植按依赖顺序走，**每一步都先在 `tools/export-goldens.js` 里导标准答案，再写 C#**：

1. `09-collision` `08-level` `24-campus` —— 几何。`Sleep.Check` 的「附近有没有床」现在是入参，这一步搬完接回去
2. `11-zombie` —— 状态机。反应延迟和定位误差是 margin 的确定函数
3. `29-power` `30-cooking` `41-injury` `40-combat` —— 各自独立，可并行
4. `33-traits` —— 只往管线挂修正，量小但连着 79 条特性
5. `20-save` —— 最后，它依赖上面所有人的序列化

搬完规则层，重心转到 **Unity 才做得到、网页做不到的事**：
遮挡低通（隔着门是「闷」不是「小声」）、真 3D 声像、混响、建模、材质。

## 不能破的规矩

1. **五层架构，依赖单向。** 第 1–3 层（配置/规则/系统）不许碰渲染引擎。
   `Campus.Core` 里零 `UnityEngine` 引用，asmdef 和测试各上了一道锁。
2. **`00-config.js` 是所有数值的唯一来源。** C# 的 `Config.g.cs` 是生成的，**不许手改**。
   改完 `00-config.js` 跑 `campus/unity/verify.sh`。
3. **声音公式不许改写**：`到达响度 = 源响度 − k×路径长度 − Σ(Portal衰减)`，
   `可听半径 = (响度 − 阈值) / k`。室内 k=2.0，室外 1.2，夜间 ×0.7。
4. **随机必须确定性**（xorshift32，注入种子）。同一个种子必须是同一所学校。
   `[实测]` JS 的 `>>` 是有符号右移，C# 的 `uint >>` 是逻辑右移 ——
   照抄会让两边从第一个数就分岔，**而且没有任何东西会报错**。
5. **移植靠对拍，不靠重写断言。** 在 C# 里重写断言只能证明「我理解得对」，
   对拍才能证明「和跑过 841 条测试的 JS 算出同一个数」。
6. **每写一段对拍，先把实现故意改坏一次，确认它真的会红。**
   `[实测]` 需求移植完 48 条全绿，把 `staminaMax()` 顺序改反 —— 还是全绿，
   因为挂在那个 key 上的特性全是加法。绿灯不等于在管事。
7. **不许写角色/特性的条件分支。** 特性只能往修正管线挂 key，
   业务逻辑永远不知道世界上存在「角色」这回事。有测试扫源码。
8. **现在不做材质和建模。** 先把玩法系统做完。

## 还欠人一个答复

伪开放世界的地图方向（看着开放、没有空气墙、本质线性），我提过三条，用户还没拍板：

- **用喊声密度当墙**：丧尸的环境低吼会叠加，玩家听得出哪片区域密。不做 UI，不做空气墙
- **能力锁替代时间锁**：用钥匙、手电、消音布来开区域，而不是用「第几天」
- **邪道收费不封堵**：天台绕路要付体力和响度的代价,但不堵死

另外 `设计文档-v3.md` 第 19 节列了 15 条需要人拍板的问题（蹲行响度、单局时长、
37 条特性未接入等），别自己替用户决定。
